# Journal: 2026-08-03-stickers

<!-- fr:journal kind=discovery scope=plan id=p1-template-two-paths-only created=2026-08-03T09:20:08 phase=1 -->
### p1-template-two-paths-only · discovery · _template frames the two resolution paths only — NOT the bracket TOKEN path (phase 1)

`_apply_template` is called from `_resolve_modifier` (bracket-path descent, named
table hit, free-form passthrough) and from `_resolve_selector_walk` (final return
+ last-step passthrough) — exactly the return points spec §Architecture-2 lists.
It is deliberately NOT called from `resolve_token`'s `name[sub]` branch: a
composition-order token like `mood[satisfied]` resolves a named chunk with no
modifier involved, and framing there was never decided.

Consequence for phases 4 and 5: the sticker composition order must name `mood`
PLAIN, as spec §1 transcribes it from `generate-stickers.py:49-54`. If an order
ever writes `mood[...]`, the frame silently disappears and the golden goes red
with a prompt that reads perfectly well.

<!-- fr:journal kind=discovery scope=plan id=p1-template-only-layer created=2026-08-03T09:20:13 phase=1 -->
### p1-template-only-layer · discovery · A _template-only dict layer (spec §1 shape) is a pure frame, and empty stays empty (phase 1)

Spec §1's config surface shows `layers: mood: {_template: "Frank's expression:
{}."}` with no table entries at all. Verified that shape works end to end: every
entry value misses the (empty) table, falls to the free-form passthrough, and
comes back framed —

    compose(["mood","scene"], {"mood": {"_template": FRAME}},
            {"mood": "sleepy", "prompt": "S"})
    -> "Frank's expression: sleepy.\n\nS"

and an entry with no `mood` field resolves to `""`, so the section is dropped
(never `"Frank's expression: ."`). Pinned by
`test_template_only_layer_is_a_pure_frame` in `tests/unit/test_compose.py`, so
phases 4 and 6 can emit that minimal layer block with confidence.

Two smaller behaviours pinned in the same group, for later phases:

- a table hit whose value is the empty string is dropped, not framed (the
  `not value` guard runs before `str.format`);
- selecting the directive key itself (`mood: "_template"`) hits the existing
  underscore guard, passes through as the literal string `_template`, and is
  then framed. The directive's VALUE never surfaces as prose — consistent with
  the pre-existing `test_underscore_keys_are_never_prose`.

<!-- fr:journal kind=finding scope=plan id=p1-mood-template-regresses-frank-covers created=2026-08-03T09:20:18 phase=1 state=open -->
### p1-mood-template-regresses-frank-covers · finding [open] · mood._template on frank shared cover layer would reframe all 84 cover prompts (phase 1)

Spec §1 puts `_template` on the layer named `mood` and says `clothing`, `mood`
and `scene` are "reused from frank's existing cover layers"; §7 has
`tools/migrate_stickers.py` emit "the `mood._template` line, for the operator to
paste into" frank's config. Read literally, that regresses every frank COVER
prompt.

Evidence (read from the live checkout `/Users/derio/Docs/projects/DERIO_NET/frank/.blog-craft.yaml`, 2026-08-03):

- `image.layers.mood` (lines 154-167) is a flat table of 13 keys whose VALUES
  already contain the whole sentence, e.g.
  `curious: Frank's expression is curious — head tilted, brow lifted, …`;
- the `hero` order (line 57) and the `banner` order (line 77) both name plain
  `mood`.

So a `_template: "Frank's expression: {}."` added to that layer frames the table
HITS as well as the sticker passthroughs, and frank's covers would compose
`Frank's expression: Frank's expression is curious — ….` — every cover, ~84
prompts. Phase 1's directive is correct and layer-name-agnostic; the collision is
in WHERE the plan declares it.

Nothing in blog-craft would catch this. Phase 5's goldens are sticker prompts
(they pass), and `tests/unit/test_image_compose.py` uses its own fixture config
with no `_template` (it passes — verified unmodified in this phase). Only frank's
own `--print-prompt` parity diff in its adoption PR would go red, i.e. after the
layer block was already pasted.

Recommended resolution for phases 4/6 (not taken here — it is a config-shape
decision, not an engine one): namespace the sticker mood layer the way spec
§Findings-2 already namespaces the other five conflicting layers —
`sticker_mood: {_template: "Frank's expression: {}."}` — and have
`composition_orders.sticker` name `sticker_mood` instead of `mood`. That also
removes the second-order hazard of a sticker whose mood text happens to equal a
cover table key (§Findings-1 notes two stickers keyed "satisfied"): under a
shared layer such a value would hit the cover table and silently compose the
cover sentence instead of the sticker's own.

`clothing` and `scene` reuse is unaffected: frank's `clothing` is a nested
two-level table, sticker clothing is free-form prose, so it passes through
unframed, and `scene` is the reserved token.

<!-- fr:journal kind=discovery scope=plan id=p1-validator-forbids-literal-braces created=2026-08-03T09:20:22 phase=1 -->
### p1-validator-forbids-literal-braces · discovery · The validator forbids ANY literal brace in _template, not just a second {} (phase 1)

`_validate_template` in `tools/validate_config.py` requires exactly one `{}` AND
exactly one `{` and one `}` overall. That is stricter than "one placeholder": it
also rejects `{0}`, `{name}`, and the `str.format` escape `{{`/`}}`. So a
`_template` cannot contain a literal brace at all.

Deliberate, and worth knowing in phase 6: the transform must not try to emit an
escaped-brace frame, and if any future frame needs a literal brace the validator
is the thing to relax (with a test), not the engine. Rationale for validating at
all is in the function docstring — a bad frame otherwise raises IndexError /
ValueError inside `generate-images.py` with a paid API call already in flight.

Non-string `_template` (including `None`, i.e. `_template:` with an empty value)
is an error too: the key being PRESENT is what triggers the check, so a
half-written directive cannot silently do nothing.

<!-- fr:journal kind=finding scope=plan id=p1-mood-template-regresses-frank-covers-resolved created=2026-08-03T09:29:12 phase=1 state=fixed -->
### p1-mood-template-regresses-frank-covers-resolved · finding [fixed] · RESOLVED: _template moved off shared mood onto a new sticker_mood layer (phase 1)

Phase 1 raised p1-mood-template-regresses-frank-covers (state open). Verified independently and CONFIRMED: frank/.blog-craft.yaml:154-167 is a flat mood table whose values are already complete sentences ("Frank\s expression is curious — head tilted..."), and the hero (line 57) and banner (line 77) orders both name plain `mood`. A _template on that layer composes "Frank\s expression: Frank\s expression is curious — ..." on every frank cover, undetected: sticker goldens are sticker-only and test_image_compose.py uses a _template-free fixture.

This was a defect in the SPEC, not in phase 1's code — §Findings divergence 1 correctly said a shared mood table cannot serve stickers, then §1 Architecture reused it anyway. Phase 1 correctly shipped the engine directive only and did not touch config shape.

Fix (orchestrator, before phase 2 dispatch):
- spec §1: order names sticker_mood; layers declare sticker_mood: {_template}; added a boxed warning explaining why, plus the consequence that entries carry modifiers.sticker_mood (not mood) because _resolve_modifier looks up entry.get(<layer name>).
- spec §1 prose: "six layers are namespaced, not five" — was five.
- spec §7 step 2: the transform emits six sticker_* layers and MUST NEVER emit a bare mood: key.
- plan 05.yaml: fixture layer set, composition order, and entry modifier key all corrected; the WARNING inlined at P5.T1.S3 so the phase-5 executor cannot reintroduce it.
- plan 06.yaml: new assertion 2b — the transform must emit no top-level mood:.
- plan 08.yaml: CONFIG.md must document the trap, not just the directive.
self-review still passes.

<!-- fr:journal kind=discovery scope=plan id=p2-post-process-target-cwd-relative created=2026-08-03T09:42:43 phase=2 -->
### p2-post-process-target-cwd-relative · discovery · post_process step target: paths are CWD-relative, not blog-root-relative (phase 2)

Discovered while asserting that --out skips post_process. `post_process()` passes a step target straight to PIL (`im.resize(...).save(str(s.get("target", output)))`, generate-images.py:182) with NO root join, while `output:` IS joined to the blog root (`out = root / e.get("output", ...)`). So a relative `target:` resolves against the PROCESS CWD.

In practice every real invocation runs from the blog root (frank/gondor/stoa call it via make/just from the repo top), so relative targets work by coincidence of cwd. A test that passes --config pointing at a tmp blog while cwd is elsewhere crashes with FileNotFoundError instead.

Consequence for later phases: any sticker/feature entry with post_process must either use paths that hold from the blog root or the caller must chdir. Phase 4 s generate-stickers.py shim passes --out, which skips post_process entirely, so it is not exposed. Left unfixed on purpose — fixing it changes resolution for existing configs and is out of this plan s scope.

<!-- fr:journal kind=finding scope=plan id=p2-archive-cap-prunes-its-own-count-run created=2026-08-03T09:49:26 phase=2 state=fixed -->
### p2-archive-cap-prunes-its-own-count-run · finding [fixed] · archive_cap FIFO prunes the variants of its own --count run (pre-existing crash, fixed here) (phase 2)

`write_archive_entry` applies the FIFO cap (`curation.archive_cap`, default 30)
on EVERY call, globbing `<key>-*.png` and unlinking all but the newest `cap`.
A `--count N` run calls it N times for the SAME key, so once N > cap the run
deletes its own earlier variants while it is still in flight.

That was already a crash on the DEFAULT path before this phase: the contact
sheet does `Image.open(p)` for every variant, so `--count 3` with
`archive_cap: 1` died with FileNotFoundError inside PIL (pinned RED first by
`test_count_above_archive_cap_without_out_dir`). It survived in practice only
because `variants[-1]` — the one the `output:` write reads — is the newest and
therefore never pruned.

`--out` widens the exposure, because it copies EVERY variant rather than only
the last. Phase 2 therefore filters `variants` to files that still exist, warns
on stderr naming `archive_cap`, and proceeds:

    live = [(lbl, p) for lbl, p in variants if p.is_file()]

Consequences for phases 4-8:

- a sticker regen with `--count N` needs `curation.archive_cap >= N` to see all
  N candidates in the `--out` dir; below that it gets fewer plus a WARN. The
  phase 4 shim generates ONE image per key (frank's script has no `--count`), so
  it is unaffected unless the shim grows a `--count` passthrough — if it does,
  the sticker config needs an `archive_cap` at least as large.
- the archive is per-key, so the cap is per-key too: 18 stickers at one variant
  each never prune one another.

<!-- fr:journal kind=discovery scope=plan id=p2-out-dir-contract-for-phase4 created=2026-08-03T09:50:16 phase=2 -->
### p2-out-dir-contract-for-phase4 · discovery · The --out contract phase 4 delegates to, and the contact-sheet placement it still cannot inherit (phase 2)

Everything phase 4's `generate-stickers.py` shim needs to know about the
`--out` contract shipped here, and the one place it still diverges from frank.

**What `--out DIR` does** (`generate-images.py`, the `if variants:` block):

- `DIR/<key>.png` — always written, last variant wins. With the default
  `--count 1` that is the ONLY file, which matches frank's `regen/` dir (one
  candidate per key per run, overwritten across runs).
- `DIR/<key>-<sha12>.png` — one per variant, written ONLY when the run produced
  more than one variant. So a plain regen does not clutter the dir the operator
  eyeballs, but a `--count 3` curation run leaves all three side by side.
- the entry's `output:` is never written, and its parent dirs are never even
  created (`test_out_dir_never_creates_the_output_path`).
- `post_process` is SKIPPED entirely. Those steps write over / next to the
  PUBLISHED asset (a step `target:` is an arbitrary path), so running them for
  a candidate would clobber shipped derivatives that `--out` promised not to
  touch.
- `.regen-archive/<key>/` snapshots + `.txt` sidecars are written exactly as
  today. The sidecar still records `output: <the entry path>` — the path that
  was NOT written. Harmless (it is provenance, and it names where a chosen
  winner belongs), but do not read it as "this file was written".
- the `DIR` argument is resolved relative to the process CWD, like
  `--reference`, not relative to the blog root. The shim should pass an
  absolute path (frank's own script resolves `--out` against its script dir,
  `generate-stickers.py:89`).

**The one remaining divergence from frank — contact-sheet placement.**
blog-craft writes the curation sheet to
`.regen-archive/<key>/contact-sheet.png`, per KEY, across that key's variants,
and only when `--count > 1`. frank writes ONE sheet per RUN across all
generated keys, into the out dir, at `regen/contact-sheet.png`, whenever at
least two stickers succeeded (`generate-stickers.py:141-144`, `cols=5,
tile_width=420`). `--out` does not change sheet placement, and it cannot: the
engine's sheet is per-key by construction.

So phase 4 cannot get frank's sheet out of `generate-images.py --out` alone. It
should call `_contact_sheet(images, out, cols=5, tile_width=420)` itself from
the shim after the delegated generation (that is exactly what phase 2 task 3
parameterized it for, and the function is importable module-level), or accept
the divergence explicitly. Recommend the former; frank's README workflow points
the operator at `regen/contact-sheet.png`.

**Model fallback / timeout, same file:** `image.fallback_model` is retried when
the primary raises OR returns a response with no image part (frank's loop only
breaks on a saved image, `generate-stickers.py:133-134`), each attempt logged to
stderr with its model name. Both models failing yields `rc=1` for that key with
NOTHING written — no empty PNG, no `output:` touch. `image.timeout_ms` is
milliseconds, straight into `genai.types.HttpOptions(timeout=...)`, matching
frank's `TIMEOUT_MS = 120_000`. With neither knob set the config object stays
`None`, so cover generation is byte-for-byte unchanged.

<!-- fr:journal kind=finding scope=plan id=p2-decision6-insufficient-for-frank-sheet created=2026-08-03T09:51:09 phase=2 state=open -->
### p2-decision6-insufficient-for-frank-sheet · finding [open] · Decision 6 alone cannot reproduce frank regen sheet: per-key-variants vs per-run-keys (phase 4 must call it) (phase 2)

Spec §Decisions 6 (and plan P2.T3) say to parameterize `_contact_sheet` "so
frank's regen contact sheet keeps its `cols=5, tile_width=420` geometry instead
of silently reflowing to 3 columns". Phase 2 did that, and it is a real
prerequisite — but it is NOT sufficient, and nothing in the spec or plan says
who supplies those arguments. Read as written, one would expect a sticker regen
through the shared engine to emit frank's sheet. It cannot.

The two sheets are different artifacts:

| | blog-craft `_contact_sheet` call site | frank `generate-stickers.py:141-144` |
|---|---|---|
| grid contents | the N variants of ONE key | the successfully generated keys of the RUN |
| how many | one per key | one per run |
| path | `.regen-archive/<key>/contact-sheet.png` | `<out>/contact-sheet.png` (i.e. `regen/`) |
| when | `--count > 1` and `curation.contact_sheet` | at least 2 keys succeeded |
| geometry | `cols=min(len,3)`, 400x260 | `cols=5, tile_width=420` |

frank's sticker run generates ONE image per key (its script has no `--count`),
so through the engine every key has a single variant and the engine's sheet is
never even produced. `--out` deliberately does not relocate the sheet: the
engine's sheet is per-key by construction and moving it would change cover
curation behavior for every blog.

Not fixed in phase 2 — the parameterization asked for is delivered and green;
the caller is phase 4's to write. Recommended resolution for phase 4: after
delegating generation with `--out`, `generate-stickers.py` calls
`_contact_sheet([(key, Image.open(regen/<key>.png)), ...], regen/contact-sheet.png,
cols=5, tile_width=420)` itself, once, over the keys it generated — the
per-run/per-key shape frank's README sends the operator to. If phase 4 instead
decides the sheet is out of scope, that is a visible behavior change for the
operator and belongs in the PR body and CHANGELOG, not in silence.

<!-- fr:journal kind=finding scope=plan id=p2-decision6-insufficient-resolved created=2026-08-03T10:42:49 phase=2 state=fixed -->
### p2-decision6-insufficient-resolved · finding [fixed] · RESOLVED: run-level sticker contact sheet moved into the phase-4 shim (phase 2)

Phase 2 raised p2-decision6-insufficient-for-frank-sheet (open). Verified independently and CONFIRMED — the two sheets are different artifacts, not one artifact at two sizes:

- engine: per-KEY across that key variants, .regen-archive/<key>/contact-sheet.png, only when --count > 1 (generate-images.py:429-431);
- frank: per-RUN across the keys generated, <out>/contact-sheet.png, whenever >=2 keys succeeded (generate-stickers.py:141-144).

frank generates one image per key, so count==1 and the engine sheet is never produced at all. Decision 6 geometry parameters cannot bridge that.

Fix (orchestrator): spec Decision 6 gains a boxed correction with a comparison table; plan phase 4 gains task 4 (P4.T4.S1 RED / P4.T4.S2 GREEN) putting the run-level sheet in the SHIM at cols=5 tile_width=420, threshold >=2 keys, explicitly forbidding any change to the engine per-key sheet. self-review passes; fr pickup --phase 4 renders the new task.

<!-- fr:journal kind=discovery scope=plan id=orch-frank-not-mounted-in-container created=2026-08-03T10:42:54 phase=2 -->
### orch-frank-not-mounted-in-container · discovery · frank repo is NOT visible inside the isolation container (blocks phase 5 as written) (phase 2)

Measured 2026-08-03. `fr isolation exec -- ls /Users/derio/Docs/projects/DERIO_NET/frank/blog/_private/frank-stickers/` -> No such file or directory. The same ls from the host worktree cwd (`cd /Users/derio/.cache/fr/worktrees/blog-craft/feat__stickers && ls ...`) succeeds and lists all six entries.

The container mounts the worktree, not sibling repos. Phase 5 steps (a) vendoring stickers.yaml and (b) deriving the 18 goldens both read frank, so they MUST run as HOST commands. Everything after vendoring is container-safe: the fixture and goldens are committed, so no test ever reaches for frank again.

Recorded as a PREREQUISITE paragraph at the head of P5.T1.S1 so the phase-5 executor does not discover it by failing.

<!-- fr:journal kind=discovery scope=plan id=p3-ladder-head-move-broke-four-assertions created=2026-08-03T11:04:02 phase=3 -->
### p3-ladder-head-move-broke-four-assertions · discovery · Adding 006_to_007 moved the ladder head to 7 and broke four assertions outside this phase scope (phase 3)

`tools/migrate_config.py:latest_version()` GLOB-discovers `migrations/NNN_to_MMM.py`,
so dropping in `006_to_007.py` silently moved the ladder head from 6 to 7. Exactly the
trap the mermaid-readability journal warned about ("any later phase adding fixtures/tests
that assert an absolute latest version should grep for `latest_version()` first"). Four
pre-existing assertions hardcoded the old head and were updated here:

- `tests/unit/test_migration_ladder.py::test_latest_is_6` -> `test_latest_is_7` (== 7),
  and it now also asserts the v7 rung ran (`features.stickers == {"enabled": False}`);
- `tests/unit/test_migration_ladder.py::test_ladder_v1_to_latest` -> version 7;
- `tests/unit/test_migration_ladder.py::test_002_to_003_preserves_explicit_palette` -> 7;
- `tests/unit/test_migration_ladder.py::test_cli_non_destructive` -> 7 after the CLI upgrade;
- `tests/unit/test_migration_005.py::test_ladder_reaches_5_from_2` -> 7.

Plus one in the validator suite: `test_out_of_range_versions_rejected` iterated
`(1, 7, "2")` — 7 is a legal version now, so the out-of-range probe above the head
became 8. A phase that extends ACCEPTED_VERSIONS must always grep that test.

The per-rung tests were NOT touched: `test_005_to_006_*` still assert `out["version"] == 6`,
because they call `005_to_006.migrate` directly. Only "reach the latest" assertions move.

For phase 5 (authors a fixture config) and phase 4 (renders configs): a NEW fixture should
be written at `version: 7`. Existing fixtures stay at their own versions on purpose —
`stoa-v1.blog-craft.yaml` (v1) is the ladder input and `valid-v2.blog-craft.yaml` (v2) is
the validator base; neither needed a change, and bumping them would destroy what they test.

<!-- fr:journal kind=discovery scope=plan id=p3-stickers-validation-semantics created=2026-08-03T11:04:51 phase=3 -->
### p3-stickers-validation-semantics · discovery · features.stickers validation semantics, and why stk-4 gating must key on enabled:false rather than absence (phase 3)

The exact contract phases 4/7/8 now code and document against
(`tools/validate_config.py:_validate_stickers`, plus the two `image` keys):

- **Presence-triggered, not None-triggered.** Every check fires on the key being IN the
  mapping, matching phase 1s `_template` choice (see
  `p1-validator-forbids-literal-braces`). So a half-written `enabled:` /
  `fallback_model:` / `timeout_ms:` — YAML `None` — is an ERROR, not a silent no-op.
- **The three path keys are required exactly when `enabled is True`.** A disabled block
  may be a stub, which is what `006_to_007` seeds. `""` and whitespace-only are rejected
  (`.strip()`), and a disabled block with `prompts_file: ""` is deliberately NOT an error
  — there is nothing to generate yet.
- **`sheet` geometry is validated whenever present, enabled or not**: `dpi` / `gutter`
  positive ints, `grid` a 2-list of positive ints `[cols, rows]`, bool guarded first
  (bool is an int subclass — same shape as `image.optimize.quality`). Bad print geometry
  is nonsense regardless of the switch, and `build-sheets.py` would otherwise discover it
  at print time.
- **`sheet.size` is deliberately UNVALIDATED** — the paper vocabulary belongs to
  `build-sheets.py` (phase 4), which must reject a size it cannot lay out. See the
  companion finding.
- `image.fallback_model`: non-empty string. `image.timeout_ms`: positive int, MILLISECONDS.
  Both absent leaves pre-v7 cover generation byte-for-byte unchanged.
- Every message names the full key path and echoes the offending value.

Cross-check pinned by `test_the_v7_migration_seed_validates`: the exact dict
`006_to_007` writes validates clean, so the migration cannot produce a config its own
validator rejects. Verified end to end too — `upgrade(stoa-v1.blog-craft.yaml)` reaches
v7 with `features.stickers == {"enabled": False}` and `validate_config` returns `[]`.

**Consequence for stk-4 (phases 4 and 8).** The row reads "a blog WITHOUT
`features.stickers` gets no sticker surface". After this rung, no updated blog is without
it: gondor and stoa get `features.stickers: {enabled: false}` written into their configs
by `/update`s ladder run. So phase 4s gating test must prove the surface is absent for
**both** shapes — block absent AND `enabled: false` — and phase 8s row notes should say
"disabled" rather than "absent", or the row will read as proven by a case that no longer
occurs in the field.

<!-- fr:journal kind=finding scope=plan id=p3-sheet-block-underspecified-for-build-sheets created=2026-08-03T11:05:49 phase=3 state=open -->
### p3-sheet-block-underspecified-for-build-sheets · finding [open] · sheet: {size, dpi, grid, gutter} does not cover frank four constants — page pixels are derived and grid is hardcoded in five places (phase 3)

Spec §5 says `build-sheets.py` is a near-verbatim port "with the four hardcoded constants
(`DPI`, `A4_W/H`, `GUTTER`, the 3x3 grid) read from `features.stickers.sheet`". Read against
frank source (`frank/blog/_private/frank-stickers/build-sheets.py`, read from the HOST — the
repo is not mounted in the container), two of those four are not reads at all. Raised here
because phase 3 owns the validation of that block and deliberately chose NOT to constrain
`size` or to couple `grid` to anything.

**1. `A4_W, A4_H = 2480, 3508` (line 23) has no config key.** The spec block is
`{size: a4, dpi: 300, grid: [3, 3], gutter: 60}` — page PIXELS are absent, so phase 4 must
DERIVE them: `round(mm / 25.4 * dpi)` off a paper table (`a4 = 210x297mm` gives exactly
2480x3508 at 300, matching frank byte dimensions). Consequences:

- the derivation must be exact for a4@300 or phase 3s print acceptance (stk-3) fails
  against frank shipped sheets;
- the `size` VOCABULARY is undefined by the spec and unvalidated by
  `tools/validate_config.py` on purpose (forward-compatible: a2/letter can be added without
  a validator change). That makes it `build-sheets.py`s job to fail LOUDLY on a size it has
  no dimensions for. A silent fallback to A4 would print the wrong page.

**2. `grid: [3, 3]` is not a constant, it is a rewrite.** `3` is hardcoded in five places:
`(A4_W - 4*GUTTER)//3` and `(A4_H - 4*GUTTER)//3` (line 29), `3*cell + 2*GUTTER` (line 30),
`range(1, 10)` (line 33) and `divmod(pos - 1, 3)` (line 40). Honoring `[cols, rows]` means
`(W - (cols+1)*G)//cols`, `cols*cell + (cols-1)*G`, `range(1, cols*rows + 1)` and
`divmod(pos-1, cols)` — and the sticker `pos` field silently changes meaning (1..9 today,
1..cols*rows). The `4 *`/`2 *` gutter multipliers are `cols+1`/`cols-1` specialized to 3,
which is easy to port wrong while still producing a plausible sheet.

Recommendation for phase 4 (not taken here — it is that phase code): keep frank a4@300,
3x3, gutter 60 as the DEFAULTS so an omitted `sheet` block reproduces frank exactly, derive
page pixels from a small explicit paper table keyed by `size`, `SystemExit` on an unknown
size, and pin the generalized geometry with a non-3x3 case (e.g. `[2, 2]`) so the
`cols+1`/`cols-1` algebra is proven rather than assumed. If phase 4 instead decides only 3x3
is supported, the validator should be tightened to say so — silently accepting `grid: [2, 4]`
that the code ignores is the worse of the two outcomes.

<!-- fr:journal kind=finding scope=plan id=p3-sheet-block-underspecified-resolved created=2026-08-03T11:14:26 phase=3 state=fixed -->
### p3-sheet-block-underspecified-resolved · finding [fixed] · RESOLVED: page dims derived from size+dpi; grid genuinely generalised and pinned (phase 3)

Phase 3 raised p3-sheet-block-underspecified-for-build-sheets (open). All three points verified and CONFIRMED.

1. Page dims had no config key. Derivation from a mm table is EXACT for frank: round(210/25.4*300)=2480 and round(297/25.4*300)=3508 are precisely frank A4_W/A4_H. Measured 2026-08-03. Adding explicit pixel keys would let size and dims disagree, so dims are derived, never configured.
2. size is deliberately unvalidated by validate_config, so build-sheets.py is the only guard and must SystemExit on an unknown size.
3. grid was decorative in the spec. frank hardcodes 3 in five places (//3 twice, 3*cell+2*GUTTER, range(1,10), divmod(pos-1,3)). Honouring [cols,rows] is a rewrite of the gutter algebra plus a change to pos range, not a constant read. A key that accepts [2,4] and lays out 3x3 is worse than no key.

Fix (orchestrator): spec 5 rewritten with the derivation, the exactness note, the SystemExit requirement and the generalised algebra; the Out-of-scope bullet that had left grid untested is replaced (it was the source of the trap). Plan 04 T1 gains tests 8, 9, 10 — including a non-3x3 case AND a [3,3] equality check against frank numbers as the regression test — and the GREEN step now specifies the algebra. Plan 04 T3 records that after v7 disabled is the common gating shape and absent the legacy one. Plan 08 gains a stale-v6 docs sweep and the stk-4 rewording. self-review passes.

<!-- fr:journal kind=finding scope=plan id=p2-review-fix1-fail-fast-restored created=2026-08-03T11:49:55 phase=2 state=fixed -->
### p2-review-fix1-fail-fast-restored · finding [fixed] · REVIEW FIX 1: the fallback loop absorbed exceptions on the DEFAULT path — fail-fast restored (phase 2)

Phases 1-3 milestone review, important. Phase 2 wrapped generate_content in try/except ... continue. With NO fallback_model configured the loop body runs exactly once, absorbs the exception and _gen_bytes returns None — so a 401, a DNS failure or an exhausted quota went from aborting the run at the first entry to WARNing once per entry and plodding through all 90+. An undeclared behavior change on the path every existing blog takes, and the opposite of what P2.T1.S1 item 5 promised (no fallback_model -> current behavior byte-for-byte).

Contract now implemented in _gen_bytes (templates/hugo-hextra/scripts/generate-images.py):
- track last_exc across attempts; after the loop, if any attempt raised, re-raise the last exception. No fallback_model -> the single attempt propagates unchanged, which IS the pre-fallback code (it called generate_content bare).
- with fallback_model: primary is still retried by the fallback (the whole point), but if all configured models raise, the last exception propagates. The fallback adds a retry; it does not downgrade a hard failure.
- an image-LESS response (no exception, no inline_data) stays SOFT: WARN, try the next model, return None -> caller prints "no image returned" and sets rc=1. That path was always soft.
- MIXED case decided here (the review did not specify it): primary raises, fallback answers without an image -> the primary exception propagates. Rationale: no model produced an image AND one of them errored, so returning None would report "the model declined" for what was really a transport/auth error; and "the fallback must not soften a hard failure" applies literally. Pinned by test_a_hard_primary_failure_is_not_softened_by_an_image_less_fallback.

Tests (tests/unit/test_generate_images_fallback.py), all RED first:
- test_no_fallback_configured_the_single_failure_propagates (the rename of the mis-named test_no_fallback_configured_failure_propagates_nothing_extra, which asserted the absorb while its name read as "nothing changed");
- test_both_models_failing_re_raises_the_last_exception — replaces test_both_models_failing_reports_and_exits_non_zero; also asserts main() propagates and output: is not created;
- test_a_hard_primary_failure_is_not_softened_by_an_image_less_fallback;
- test_image_less_responses_stay_a_SOFT_failure — None both with and without a fallback, plus main() rc==1 and nothing written;
- test_primary_failure_falls_back_to_the_fallback_model (pre-existing) still proves the fallback IS attempted when the primary raises.

RED: 4 failed / 17 passed. GREEN: 21 passed.

<!-- fr:journal kind=finding scope=plan id=p2-review-fix7-warn-names-exception-type created=2026-08-03T11:50:12 phase=2 state=fixed -->
### p2-review-fix7-warn-names-exception-type · finding [fixed] · REVIEW FIX 7: the attempt WARN now names the exception type, not just a truncated str() (phase 2)

Phases 1-3 milestone review, minor. The fallback WARN printed str(exc)[:160] only. Many SDK/transport exceptions have an empty or uninformative str() (socket.timeout, some google.api_core errors), and on the default path the operator previously got a full traceback — so catching and printing only the message could leave the failure undiagnosable.

Now: WARN: <model> error: <TypeName>: <str(exc)[:160]>. With FIX 1 the exception propagates anyway (traceback restored), but the WARN still fires for every attempt whose exception is NOT the one re-raised — the primary in a two-model run — so its type is the only record of it.

Pinned inside test_primary_failure_falls_back_to_the_fallback_model: assert "RuntimeError" in err alongside the existing model-name and message assertions. RED first (the type was absent).

<!-- fr:journal kind=finding scope=plan id=p2-review-fix2-out-filename-contract created=2026-08-03T11:55:15 phase=2 state=fixed -->
### p2-review-fix2-out-filename-contract · finding [fixed] · REVIEW FIX 2: --out now writes the output: BASENAME, key-prefixed on a collision in the selected set (spec 5a) (phase 2)

Phases 1-3 milestone review, important. Phase 2 hardcoded dest = out_dir / f"{key}.png" — both the stem AND the extension. Two measured facts (2026-08-03) rule out each obvious option:

- <key>.png loses frank naming: frank regen files are sticker-<key>.png (generate-stickers.py:118), the same name as the master its README says to copy over, so the runbook breaks;
- the bare basename collapses covers: 85 of frank 91 cover entries have output: ending /cover.png, so a multi-key --out run would write one file 85 times.

Implemented per spec 5a in a new module-level helper _out_dir_names(selected) (templates/hugo-hextra/scripts/generate-images.py):
- name = basename of the entry output:;
- if two or more entries IN THE SELECTED SET share that basename, EVERY member of the colliding group becomes <key>-<basename> (never one bare + the rest prefixed, which would make who got the bare name depend on iteration order);
- computed for the whole selected set BEFORE any generation, so it is a pure function of the set;
- extension always comes from output:, so a .webp entry lands as .webp.

Structural change that made this possible: main() now resolves the selected set up front (only/operator_generated/empty-prompt filters + compose_for) into a list of (key, entry, prompt, out) and then iterates that. compose_for is pure and API-free, so composing before generating changes no behavior — and the alias guard (FIX 4) needs the same up-front set.

For stickers this yields exactly sticker-<key>.png. NOTE for phase 4: --only narrows the SELECTED set, so a single cover entry regenerated alone keeps the bare cover.png; the prefix only appears when there is something to disambiguate from. Pinned by test_a_single_selected_entry_never_collides_with_itself.

Tests (tests/unit/test_generate_images_out_dir.py), all RED first: test_out_dir_uses_the_output_basename, test_colliding_basenames_are_all_key_prefixed, test_the_collision_decision_is_order_independent (same three entries in two orders -> identical names), test_a_single_selected_entry_never_collides_with_itself, test_a_non_png_output_keeps_its_real_extension. The per-variant curation files keep their archive names (<key>-<sha>.png), which are unique by construction. RED 8 failed/14 passed -> GREEN 22 passed.

<!-- fr:journal kind=finding scope=plan id=p2-review-fix4-out-refuses-to-alias-output created=2026-08-03T11:55:36 phase=2 state=fixed -->
### p2-review-fix4-out-refuses-to-alias-output · finding [fixed] · REVIEW FIX 4: --out refuses the run when its directory aliases any output:, instead of writing one (phase 2)

Phases 1-3 milestone review, minor by likelihood, load-bearing by consequence. --out DIR is resolved against the process CWD, so --out static/images run from the blog root makes dest resolve to the very file output: names — and the guarantee the flag exists for (never writes output:) quietly evaporates. A path-shaped promise is not a guarantee.

Now: before generating anything, main() compares each entry --out destination against the resolved output: paths of the WHOLE selected set (new helper _output_alias). On any hit it prints one ERROR per offender naming BOTH paths, says whether it is that entry own output: or another key, and returns 1 — no API call, no write.

Detection is resolved-path equality (relative vs absolute, .., a symlinked out dir) PLUS Path.samefile when both exist (two real paths on one inode, i.e. a hard link, which resolution cannot see). Checked against every selected entry output:, not just the entry own, because under the FIX 2 collision renaming a <key>-<basename> could in principle land on a different entry published path.

The guard is per-FILE, not per-tree: --out static/images/regen is legal and stays legal (pinned), so an operator can keep candidates near the published tree.

Tests (RED first): test_out_dir_that_aliases_output_is_refused (non-zero rc, master byte- AND mtime-unchanged, both paths in stderr), test_the_alias_guard_sees_through_a_symlinked_out_dir, test_a_distinct_out_dir_under_the_published_tree_is_fine.

<!-- fr:journal kind=finding scope=plan id=p2-review-fix5-bare-name-written-once created=2026-08-03T11:55:54 phase=2 state=fixed -->
### p2-review-fix5-bare-name-written-once · finding [fixed] · REVIEW FIX 5: the --out bare-name file was written once per VARIANT; hoisted below the loop (phase 2)

Phases 1-3 milestone review, minor. dest.write_bytes(raw) sat inside the per-variant copy loop, so a --count N run wrote the reviewable file N times, N-1 of them immediately overwritten — N-1 wasted writes and a window in which the file held a candidate that is not the one the run reports.

Fixed: the per-variant <key>-<sha>.png copies stay in the loop (guarded by len(variants) > 1 as before); the bare name is written once, below the loop, from variants[-1] — the same bytes the default path writes to output:, and the same one it prints.

Pinned by test_the_bare_name_is_written_once_per_run: monkeypatches Path.write_bytes to record every write during a --count 3 run and asserts the bare-name path appears EXACTLY once, with the last variant bytes. RED first (three writes recorded).

<!-- fr:journal kind=finding scope=plan id=p2-review-fix3-two-missing-pins created=2026-08-03T11:56:10 phase=2 state=fixed -->
### p2-review-fix3-two-missing-pins · finding [fixed] · REVIEW FIX 3: the two --out pins phase 2 owed — --reference combination, and the contact sheet NOT moving (phase 2)

Phases 1-3 milestone review, important (missing coverage, not a defect: both behaviours were already correct and both new tests were GREEN on first run, which is the point — they were unpinned).

1. test_out_dir_combines_with_reference — the plan asked for the combination and it was untested. Under TEST_MODE the reference file is never opened, so the observable proof that --reference reached the generation path is the archive SIDECAR: it records reference: <path>. Asserts that, plus dest written and the curated output: byte-unchanged.

2. test_out_dir_does_not_relocate_the_contact_sheet — --out does NOT move the per-key sheet out of .regen-archive/<key>/contact-sheet.png. Phase 4 shim is being built on exactly this non-movement (it builds frank run-level sheet itself by calling _contact_sheet(..., cols=5, tile_width=420) — Decision 6 correction), so the placement is a contract phase 4 depends on and now has an assertion rather than a journal paragraph. Asserts the sheet exists under .regen-archive AND that no contact-sheet.png exists anywhere under the --out dir (rglob).

<!-- fr:journal kind=finding scope=plan id=p2-review-fix6-contact-sheet-cols-zero created=2026-08-03T11:56:27 phase=2 state=fixed -->
### p2-review-fix6-contact-sheet-cols-zero · finding [fixed] · REVIEW FIX 6: _contact_sheet(cols=0) silently became 3; None now means default, 0 is an error (phase 2)

Phases 1-3 milestone review, minor. cols = min(len(images), cols or 3) folded None and 0 (and any falsy value) into the same meaning. A caller that COMPUTES its column count — which is exactly what phase 4 build-sheets.py / the shim do from features.stickers.sheet.grid — and gets 0 would silently produce a 3-column sheet instead of failing.

Now explicit: cols is None -> 3 (the historical default, unchanged); cols < 1 -> ValueError naming cols; then the existing clamp to the image count. No behaviour change for any legal call.

Chose an exception over clamping to 1: a zero column count means the caller arithmetic is wrong, and a one-column sheet would be a plausible-looking artifact hiding that. Pinned by test_cols_zero_is_rejected_rather_than_silently_defaulting (0 and -1 raise, and no file is written) plus test_cols_none_still_means_the_default_three. RED first: 1 failed/8 passed -> 9 passed.

<!-- fr:journal kind=finding scope=plan id=p1-review-fix8-template-docstring-overclaimed created=2026-08-03T11:59:22 phase=1 state=fixed -->
### p1-review-fix8-template-docstring-overclaimed · finding [fixed] · REVIEW FIX 8: the _template validator docstring said the rejected cases raise; three of them format fine (phase 1)

Phases 1-3 milestone review, minor, docs-only (no behaviour change). tools/validate_config.py:_validate_template documented the rule purely as crash-prevention — no {} drops the value, two {} raise IndexError, a stray brace raises ValueError. But the check also rejects {0}, {:>10} and the legal str.format escapes {{ / }}, all of which format PERFECTLY WELL with one positional arg. A future reader taking the docstring at face value would find those cases and "fix" the rule as a bug.

Corrected wording separates the two reasons explicitly: (1) frames that WOULD misbehave mid-run with a paid API call in flight (adds {name} -> KeyError, which was also missing); (2) frames that are legal str.format but forbidden BY POLICY — no literal brace anywhere — so a frame has exactly one obvious spelling, stays diffable, and is mechanically emittable by tools/migrate_stickers.py. Plus the consequence: if a frame ever legitimately needs a literal brace, relax the VALIDATOR with a test, never the engine.

Also given teeth in tests/unit/test_config_schema.py: test_template_with_stray_braces_rejected now covers only the genuinely unmatched braces, and a new test_legal_but_policy_rejected_braces asserts BOTH halves — legal.format("VALUE") does not raise AND validate_config still rejects it — for {{}} literal {}, {0} and |{:>10}|. The section comment above them carries the same distinction.

tools/compose.py _apply_template carried a milder version of the same claim ("exactly one {} and no stray braces ... would otherwise raise"); corrected and re-mirrored to templates/hugo-hextra/scripts/compose.py. test_mirrors passes (119 passed across mirrors + config_schema + compose + image_compose).

<!-- fr:journal kind=finding scope=plan id=p1-review-fix9-bracket-token-on-template-layer created=2026-08-03T12:01:16 phase=1 state=fixed -->
### p1-review-fix9-bracket-token-on-template-layer · finding [fixed] · REVIEW FIX 9: validate_config now REJECTS an order token X[y] whose layer X declares _template (phase 1)

Phases 1-3 milestone review, minor. _template is applied on the two resolution paths (_resolve_modifier, _resolve_selector_walk) and deliberately NOT on resolve_token name[sub] branch (journal p1-template-two-paths-only). But X and X[y] look identical in config and behave differently, and the failure mode is silent: the prose section simply loses its frame — no exception, a prompt that still reads perfectly well, and for the stickers that means every sticker composing without "Frank expression: ..." while the goldens are the only thing that would notice.

ERROR, not a warning — the judgement the review asked me to state, with the reasons:
1. there IS no warning channel. validate_config returns list[str] and the CLI exits 1 on any element; inventing a severity tier for this one rule is a bigger change than the rule.
2. nothing downstream can detect it. compose() cannot tell an intentional bracket token from a mistaken one, and the engine hardcodes no layer vocabulary, so there is no later gate.
3. the author has two cheap correct spellings, both named in the message: name the layer plain, or move the frame onto a layer that is never bracket-addressed. Nothing legitimate is blocked.
4. zero deployed configs can trip it: _template ships in THIS branch, so no existing blog config has one. The rule cannot break an existing blog.

Implementation: in the token loop of validate_config (tools/validate_config.py) — the missing-layer branch now short-circuits with a loop continue, then a bracket token (base != tok) on a dict layer declaring _template appends the error, naming the order, the token, the layer, and both remedies. Applies to the v4 single composition_order and to every v5 named order.

Tests (RED first, 2 failed/3 passed -> all green): test_bracket_token_naming_a_template_layer_rejected (v4 path), test_bracket_token_on_a_template_layer_rejected_in_a_named_order (v5, and asserts a SECOND order naming the layer PLAIN is not flagged), test_a_plain_token_on_a_template_layer_is_fine, test_a_bracket_token_on_a_frameless_layer_is_still_fine (the pre-existing reference_guidance[anchor] idiom is untouched).

Consequence for phases 4-6: this now MECHANICALLY enforces what p1-template-two-paths-only could only warn about — a sticker composition order that writes sticker_mood[...] fails validation instead of shipping an unframed prompt. compose.py module docstring records the rule alongside the directive (re-mirrored).

<!-- fr:journal kind=discovery scope=plan id=p4-sheet-geometry-verified-against-shipped-artifact created=2026-08-03T14:37:21 phase=4 -->
### p4-sheet-geometry-verified-against-shipped-artifact · discovery · build-sheets.py page + DPI verified against frank's SHIPPED sheet (IHDR 2480x3508, pHYs 11811) (phase 4)

The generalised algebra reproduces frank exactly, and the derivation is *verified
against the shipped artifact*, not just against his source constants. Read from
the host (frank is not mounted in the container), `frank/blog/_private/frank-stickers/sheets/frank-stickers-A4-sheet1.png`
carries `IHDR 2480 3508` and `pHYs 11811 11811 1` — i.e. 11811 px/m on both axes.

- `round(210 / 25.4 * 300) == 2480`, `round(297 / 25.4 * 300) == 3508` — exactly IHDR.
- `page.save(dest, dpi=(300, 300))` writes `pHYs 11811 11811 1` — exactly frank's.
- `[3, 3]` geometry: `cell = 746`, `off_x = 61`, `off_y = 575`, identical for
  frank's hardcoded `min((W-4G)//3, (H-4G)//3)` / `3*cell+2*G` and for the
  generalised `min((W-(cols+1)G)//cols, (H-(rows+1)G)//rows)` / `cols*cell+(cols-1)G`.
  Pinned twice: numerically (`_frank_geometry() == _generalised(3, 3) == (746, 61, 575)`)
  and by output equality (default vs explicit `grid: [3, 3]` produce byte-identical PNGs).
- non-3x3 pinned with `[2, 4]`: `cell = 802`, `off = (408, 60)`, `pos` valid 1..8.

So stk-3's print acceptance rests on measured equality with the artifact the
operator actually prints, not on a transcription.

<!-- fr:journal kind=finding scope=plan id=p4-two-false-assertions-in-plan-step created=2026-08-03T14:37:57 phase=4 state=fixed -->
### p4-two-false-assertions-in-plan-step · finding [fixed] · P4.T1.S1 asked for two assertions that are false: PNG dpi reads back 299.9994, and 600dpi A4 width is 4961 not 4960 (phase 4)

Two assertions P4.T1.S1 asks for verbatim are FALSE as written. Both were caught
by arithmetic/measurement while writing the RED test, and both are pinned with the
corrected wording rather than the plan's.

**1. `dpi == (300, 300)` read back from Pillow is not literally true.** PNG stores
resolution in the `pHYs` chunk as *integer pixels per metre*, so `dpi=(300, 300)`
is written as 11811 px/m and Pillow reads it back as `(299.9994, 299.9994)`
(measured, Pillow 12.3.0). A test asserting `im.info["dpi"] == (300, 300)` would
fail on a file that is exactly right — and, worse, an implementer chasing that
assertion could "fix" it by writing a wrong `pHYs`. The test therefore asserts
`tuple(round(d) for d in dpi) == (300, 300)` PLUS the exact chunk
`_pHYs(p) == (11811, 11811, 1)`, which is byte-identical to frank's committed sheet.
The integer chunk is the real print contract; the float is a derived convenience.

**2. "Assert a4+600dpi doubles them" is off by one pixel.** `round()` is not
linear: `round(210/25.4*300) == 2480` but `round(210/25.4*600) == 4961`, not 4960
(`210/25.4*600 = 4960.63`). The height does double (`round(297/25.4*600) == 7016
== 2*3508`) which is exactly what makes the trap plausible. The test asserts the
DERIVATION (`(4961, 7016)`), not a scale factor, and says so in its docstring.

Neither is a code defect — the implementation was written to the derivation from
the start. They are plan-text defects that would have produced a red suite or a
wrong "fix". Consequence for phase 8: any doc sentence of the form "at 600 dpi the
sheet is twice as large" is wrong; say "derived per axis, `round(mm/25.4*dpi)`".

<!-- fr:journal kind=finding scope=plan id=p4-duplicate-sheet-pos-silently-drops-a-sticker created=2026-08-03T14:38:13 phase=4 state=open -->
### p4-duplicate-sheet-pos-silently-drops-a-sticker · finding [open] · build-sheets.py inherits frank's last-wins on a duplicate (sheet,pos) — a sticker silently vanishes from a PRINTED sheet (phase 4)

`build-sheets.py` keeps frank's `sheets[s["sheet"]][s["pos"]] = key` shape, so two
stickers claiming the same `(sheet, pos)` mean the LAST one silently wins and the
other is absent from a printed page — paper, ink and cutting spent on a sheet that
is missing a sticker, with no warning anywhere. `pos` out of range IS now loud (the
generalised grid made that a required change); a duplicate is not.

Measured: frank's own 18 stickers occupy 18 distinct `(sheet, pos)` pairs
(sheets 1 and 2, pos 1..9 each), so nothing in the port is affected today.

NOT fixed here, deliberately: the guard is three lines
(`if pos in sheets[n]: raise SystemExit(...)`), but rejecting a duplicate is a
behaviour change against a config that "prints fine" today, and P4.T1 neither asks
for it nor pins it. Recommended for the orchestrator to fold into phase 8 (or a
follow-up) as an explicit decision, with a test, rather than for me to slip in an
untested behaviour. If the decision is to keep last-wins, it should at least WARN.

<!-- fr:journal kind=finding scope=plan id=p4-order-sticker-must-be-bracketed-reference created=2026-08-03T14:44:10 phase=4 state=open -->
### p4-order-sticker-must-be-bracketed-reference · finding [open] · PHASE 5 BLOCKER: entries must say order: composition_orders[sticker]; a bare 'sticker' composes an empty prompt and the engine SILENTLY skips the entry (phase 4)

Plan `05.yaml` P5.T1.S3 says each sticker entry carries
`composition.{order: sticker, ...}`, and the orchestrator's phase-4 brief repeats
it. That spelling composes an EMPTY prompt and the engine then SKIPS the entry in
silence — all 18 stickers, zero output, exit code 0.

Mechanism (`templates/hugo-hextra/scripts/generate-images.py:99-119`):

    _ORDER_REF = re.compile(r"^composition_orders\[([A-Za-z0-9_-]+)\]$")
    if isinstance(o, str):
        m = _ORDER_REF.match(o.strip())
        return orders.get(m.group(1), []) if m else []      # <-- bare "sticker" -> []

`order_tokens` returns `[]`, `compose([])` returns `""`, and `main()` has
`if not prompt.strip(): continue` — so the entry silently drops out of the selected
set. No warning, no non-zero rc, and under `--out` not even a file to notice is
missing. Only `tools/validate_images.py:50-58` catches it ("unknown composition
order reference"), and only if someone runs the validator.

Correct spelling, used by the fixture in `tests/unit/test_generate_stickers_shim.py`
and by every existing v5 example in the repo (`docs/CONFIG.md:111`,
`tests/unit/test_generate_images_v5.py:81`):

    composition:
      order: composition_orders[sticker]

**Consequence for phase 5.** If the golden fixture writes `order: sticker`, the
18 `--print-prompt` assertions fail with an empty string — which reads like "the
prompt composed wrong" and sends the executor hunting through layers, framing and
the border/scene ordering, when the defect is one token in the entry. Phase 5
should also consider asserting `tools/validate_images.py` returns no errors for
the fixture, which turns this whole class of entry-shape mistake loud.

Not fixed in code: the engine's tolerance of an unresolvable order is pre-existing
and shared with covers, and tightening it (WARN or rc=1 on an empty composed
prompt) is an engine decision outside this phase. Recorded so phase 5 does not
have to discover it by failing, and so the orchestrator can correct 05.yaml.

<!-- fr:journal kind=discovery scope=plan id=p4-shim-shadow-config-and-regen-location created=2026-08-03T14:44:32 phase=4 -->
### p4-shim-shadow-config-and-regen-location · discovery · The shim's two open choices: a shadow config co-located with the real one, and regen/ moving to the config root (phase 4)

Two implementation choices the plan left open, both settled here with the reasoning,
because both are visible to the operator or to a later reader.

**1. How the shim makes the engine read the STICKER prompts file.** The engine
reads `image.prompts_file` and has no flag to override it (`--config` is the only
input path). Adding a `--prompts-file` flag would be an engine change phase 2 did
not sanction, so the shim writes a SHADOW CONFIG — a copy of the real config with
`image.prompts_file` replaced — and passes that to `gi.main(["--config", shadow, ...])`.

The shadow must live in the SAME DIRECTORY as the real config, and this is the
non-obvious part: the engine sets `root = cfg_path.parent` and resolves `output:`,
every reference path, and the `--out` alias guard against that root. A shadow in a
temp directory would silently relocate all of them — references would all WARN
"not found", and the alias guard (phase-2 review fix 4, the guarantee that `--out`
never writes a published asset) would compare against paths under `/tmp` and pass
vacuously. So: `tempfile.mkstemp(prefix=".blog-craft.stickers-", suffix=".yaml",
dir=cfg_path.parent)`, unlinked in `finally`. `find_config` matches
`.blog-craft.yaml` exactly, so the shadow can never be mistaken for a config.

If a future phase wants this cleaner, the honest fix is a `--prompts-file` flag on
the engine, not a smarter temp file.

**2. `regen/` MOVES, and the runbook moves with it.** frank resolved `--out`
against his private script directory (`HERE / args.out`, generate-stickers.py:89),
so candidates landed in `blog/_private/frank-stickers/regen/`. The shipped script
lives at `<site_dir>/scripts/`, so that base is gone; a relative `--out` now
resolves against the CONFIG ROOT, i.e. `<repo>/regen/`. (It must become absolute
before reaching the engine, where `--out` is CWD-relative like `--reference` —
pinned by `test_regen_resolves_against_the_config_root_not_the_cwd`, which runs the
shim from a directory outside the blog.)

Measured: neither `regen/` nor `blog/_private/frank-stickers/regen/` is gitignored
in frank today (`git check-ignore` returns 1 for both), so this is a location
change, not a new tracking hazard. It IS a runbook change — frank's README points
the operator at `regen/contact-sheet.png` next to the script — so phase 8 should
name it in the CHANGELOG alongside the contact-sheet layout divergence, and frank's
adoption PR (spec 8) should either accept `<repo>/regen/` or pass `--out
blog/_private/stickers/regen` explicitly.

<!-- fr:journal kind=finding scope=plan id=p4-order-spelling-blocker-resolved created=2026-08-03T13:07:55 phase=4 state=fixed -->
### p4-order-spelling-blocker-resolved · finding [fixed] · RESOLVED: plan 05 now says composition_orders[sticker]; empty-prompt silence becomes a WARN (phase 4)

Phase 4 raised p4-order-sticker-must-be-bracketed-reference as a PHASE 5 BLOCKER. Verified independently: _ORDER_REF at generate-images.py:99 is ^composition_orders\[([A-Za-z0-9_-]+)\]$; a non-matching string returns [], compose([]) returns empty, and main() line 439 skips it. A bare order: sticker therefore yields all 18 stickers, zero output, exit 0.

The defect was mine — plan 05.yaml:41 and my phase-4 brief both said order: sticker.

Fixed: 05.yaml now specifies composition_orders[sticker] with the full mechanism inline so the phase-5 executor cannot reintroduce it, and gains an assertion that tools/validate_images.py reports no errors for the fixture (the only thing that catches an unresolvable order). New spec 5b records the entry shape. Phase 8 task 4 adds a WARN naming the key when a composed prompt is empty — deliberately a warning, not an error, because an operator may have a legitimately empty entry and changing the exit code would be a real behaviour change on the cover path.

<!-- fr:journal kind=decision scope=plan id=p4-duplicate-pos-guard-decided created=2026-08-03T13:08:39 phase=4 -->
### p4-duplicate-pos-guard-decided · decision · DECIDED: a duplicate (sheet,pos) becomes a hard error, scheduled as phase 8 task 4 (phase 4)

Phase 4 left p4-duplicate-sheet-pos-silently-drops-a-sticker open, correctly declining to slip an untested behaviour change into a phase that neither asked for it nor pinned it.

Decision: make it loud. build-sheets.py exits non-zero naming both keys and the position.

Reasoning. The failure is expensive and invisible in a way software failures usually are not — last-wins means a sticker is absent from a sheet the operator PRINTS and CUTS, so paper, ink and manual work are spent before anyone notices. That asymmetry outweighs the usual preference for tolerating odd input. It is a behaviour change against frank code, but an unobservable one: frank 18 stickers occupy 18 distinct (sheet,pos) pairs across sheets 1-2 (measured), so no existing config regresses. Declared in the CHANGELOG regardless, per the standing rule that no behavioural change ships undeclared.

Scheduled as phase 8 task 4 alongside the empty-prompt WARN, with RED/GREEN steps and a test that frank real 18 still build both sheets.

<!-- fr:journal kind=discovery scope=plan id=p5-goldens-byte-identical-first-run created=2026-08-03T15:36:42 phase=5 -->
### p5-goldens-byte-identical-first-run · discovery · THE GATE HELD: all 18 goldens byte-identical on the FIRST green run, no fixture iteration (phase 5)

The phase-5 equality proof passed with zero iteration. Every measured fact in the plan and the orchestrator brief was re-verified and held; nothing in the port had to be adjusted to make a golden match, and no golden was touched after derivation.

Derivation (host commands, frank is not mounted in the container). The stub recipe from the brief worked as given: five module stubs (google, google.genai, PIL, lib, lib.contact_sheet), importlib-load frank generate-stickers.py, then call compose_prompt and scene_refs against the VENDORED yaml rather than frank own copy, so the committed goldens are provably the composition of the committed fixture. Mechanized as tests/fixtures/stickers/derive-goldens.py with a --check mode that re-derives and diffs, since the derivation cannot run in CI.

Verified before committing, all confirming the brief: 18 prompts, each exactly 8 blank-line separated sections and each of the 8 non-empty; each golden equals the 8 pieces read straight out of the vendored yaml joined in frank order, with sticker_border_spec AFTER scene; refs 4 when clothing_anchor is set and 3 when null (12 and 6 respectively); lengths 4628 to 4697 characters; 22-coffee-b carries the exact mood line as section 6 of 8.

One invariant worth naming because the equality rests on it and nothing else states it: frank join does NOT filter empty sections while blog-craft compose() DOES. The two agree only because all eight sections are non-empty for all 18 stickers. That is now an assertion (test_every_golden_has_eight_non_empty_sections), not an assumption — a 19th sticker with an empty clothing string would compose one section fewer in blog-craft than in frank, and only that test would say so.

The bracketed-order trap never fired, because the brief pre-empted it. Its cost is confirmed though: mutating the fixture to a bare order: sticker reddens all 18 with a one-section prompt plus the validate_images assertion, which is exactly the misleading signal the correction avoided.

Both new files were mutation-checked (mutations reverted, worktree byte-restored and verified with cmp): swapping scene and sticker_border_spec reddens 19 assertions; moving the clothing anchor to the front of the reference payload reddens 48 of the reference assertions while all 28 golden-prompt assertions stay GREEN. That last pair is the proof that the two tasks cover different classes: prompt equality is blind to payload order, and payload order is what tells the model which picture Frank face comes from.

Full suite after both tasks: 1082 passed, 7 skipped, 8m45s. Baseline before this phase was 979 passed + 7 skipped, so the phase adds 103 assertions. tests/unit/test_image_compose.py is untouched (git diff empty) and green.

<!-- fr:journal kind=finding scope=plan id=p5-t2-cannot-be-red-mapping-belongs-to-t1 created=2026-08-03T15:37:02 phase=5 state=open -->
### p5-t2-cannot-be-red-mapping-belongs-to-t1 · finding [open] · P5.T2.S1 cannot be RED as written: the reference mapping is a prerequisite of T1.S3, not a follow-on (phase 5)

P5.T2.S1 says EXPECT: RED. It cannot be, and no ordering of the two tasks fixes it — the plan itself puts the mapping in the earlier step.

Mechanism. T1.S3 specifies the entry shape as composition.{order, modifiers, scene, reference_images}, i.e. reference_images is part of the fixture T1 must author. It is also load-bearing for T1 two extra assertions: validate_images flags a reference path that does not resolve (validate_images.py:102-104), so a fixture with no reference_images either fails that assertion or passes it vacuously. By the time T1 is green, the mapping T2.S2 asks for already exists, and T2.S1 is therefore green on first run.

This is the same shape as phase 2 review fix 3 (two tests that were GREEN first run because the behaviour was already correct and merely unpinned) — missing coverage rather than a defect. Recording it because a reader comparing the plan EXPECT lines against the commits would otherwise read a green T2.S1 as TDD discipline skipped.

What was done instead of a synthetic RED: mutation testing, with each mutation reverted and the file byte-restored (cmp verified). Moving the clothing anchor to the front of the payload reddens 48 of the 75 reference assertions; reversing the two style anchors reddens the per-key payload assertions. Crucially all 28 golden-prompt assertions stay GREEN under those mutations, which is the positive demonstration that T2 covers a class T1 cannot see: the prompt is identical whichever order the images are attached in, while the composed reference_guidance prose declares the FIRST image the face authority. A payload led by a clothing anchor is a real, silent defect that only T2 catches.

Recommendation for the orchestrator, not taken here (it is plan text, not code): either fold reference_images out of T1.S3 entry shape so T2 has something to make green, or relabel T2.S1 as a coverage step rather than a RED step. The second is more honest — the mapping genuinely belongs with the entry shape, and splitting it just to manufacture a red would make T1 fixture temporarily invalid.

<!-- fr:journal kind=discovery scope=plan id=p5-fixture-artifacts-for-phases-6-8 created=2026-08-03T15:37:35 phase=5 -->
### p5-fixture-artifacts-for-phases-6-8 · discovery · What phase 5 leaves for phases 6-8: the fixture config, the shadow-config trick, the golden format, and three path choices (phase 5)

Everything a later phase needs to know about the phase-5 artifacts, and the three choices that were open.

ARTIFACTS (all under tests/fixtures/stickers/):
- frank-stickers.yaml, frank stickers.yaml verbatim, sha256 723c2b5b..., from frank HEAD 0aa4095c (the file itself last changed in 1373d06e).
- golden/<key>.txt x18 — the prompt PLUS ONE TRAILING NEWLINE, deliberately, so the file is exactly what generate-images.py --print-prompt writes on stdout (print adds it). The test compares raw bytes with no normalisation step to get wrong.
- golden/<key>.refs.txt x18 — one reference BASENAME per line, payload order, primary first. Basenames because the directories move in the port while the order is the thing under test.
- GOLDEN-PROVENANCE.md — frank sha, the five stubs and why each is safe, why --dry-run could not be used.
- derive-goldens.py — the derivation, with --check to diff a re-derivation against the committed set. Needs a frank checkout, so never runs in CI.
- blog-craft.yaml — the fixture config: the shape frank pastes. GENERATED from the vendored prose, never retyped.

FOR PHASE 6 (tools/migrate_stickers.py). The mechanical mapping frank record to v5 entry lives in tests/unit/_stickers_fixture.py (sticker_entries and reference_paths) and is the same transform phase 6 ships. The transform output should be diffable against it, and tests/fixtures/stickers/blog-craft.yaml is the layer block it must emit — including the six namespaced layers, sticker_mood as a _template-only layer, clothing as an EMPTY table, and NO top-level mood. Note clothing: {} is not decorative: resolve_layer returns empty string for a layer that is absent from image.layers and compose() then drops the section, so omitting clothing silently deletes the clothing sentence from all 18 prompts. Test test_clothing_is_an_empty_table_so_sticker_prose_passes_through pins it.

THE SHADOW CONFIG IS ALSO A TEST TECHNIQUE. Neither generate-images.py --print-prompt nor tools/validate_images.py has a prompts-file flag; both read image.prompts_file. So the fixture blog carries the real config at .blog-craft.yaml (features.stickers shape, validated by validate_config) plus shadow.blog-craft.yaml, a copy with image.prompts_file pointed at the sticker prompts file, in the SAME directory because the engine resolves the blog root as cfg_path.parent. That is exactly what the shipped shim does at runtime (journal p4-shim-shadow-config-and-regen-location). Any later phase wanting to run the engine over sticker entries needs the same two-file setup, or the engine needs the --prompts-file flag that journal already names as the honest fix.

THREE PATH CHOICES, all made toward less invention:
1. features.stickers.{prompts_file,images_dir,sheets_dir} in the fixture point at frank CURRENT dirs (blog/_private/frank-stickers/...), not spec section 1 blog/_private/stickers/. Reason: frank style_anchors are themselves sticker masters under his images dir, so his vendored reference strings resolve unchanged. Phase 6 is what relocates them, and when it does it must rewrite those two style_anchor paths too or the anchors go dead.
2. The prompts file is named stickers-prompts.yaml, distinct from the vendored stickers.yaml, so the two are never confused in a fixture tree that holds both.
3. frank scene_refs FILTERS by existence, so the committed refs goldens reflect frank real disk state. All 12 clothing anchors and both style anchors exist today, which is why the counts are a clean 4/3. If one were ever missing, frank would have silently sent 3 and the golden would record 3 — the goldens are a record of frank behaviour, including that tolerance.

FOR PHASE 8 (matrix). stk-1 is now verifiable: levels unit becomes blog-craft:tests/unit/test_stickers_golden.py and status moves not-implemented to ci. Row stk-4 second half (no change to composed cover prompts) is carried by test_image_compose.py passing unmodified, which it does. The matrix was deliberately NOT edited in this phase — the plan assigns the flips to phase 8 and a second edit here would only conflict.

<!-- fr:journal kind=discovery scope=plan id=p6-transform-reproduces-all-18-goldens created=2026-08-03T14:17:54 phase=6 -->
### p6-transform-reproduces-all-18-goldens · discovery · The transform IS prompt-preserving: 18 goldens byte-identical through the real tool, and the emitted layer block is byte-equal to the phase-5 fixture (phase 6)

The phase-6 claim held. tests/unit/test_migrate_stickers.py runs the SHIPPED tool as a subprocess against a blog in franks pre-migration state, pastes the block the tool printed into a shadow config exactly as spec section 8 step 3 tells the operator to, and then compares generate-images.py --print-prompt output for all 18 keys against the committed goldens as raw bytes. All 18 match. Nothing in the fixture or the goldens was adjusted to make that happen.

Two equalities beyond the prompts, both explicitly asserted:

1. THE LAYER BLOCK IS BYTE-EQUAL TO tests/fixtures/stickers/blog-craft.yaml. test_the_printed_block_is_byte_identical_to_the_phase5_fixture_config slices the fixture file from the line starting with two spaces then composition_orders: up to the line starting features:, and compares it to the tools printed block minus its leading image: line. Exact match. So the fixture the phase-5 gate composes from and the shape the shipped tool tells frank to paste are the same bytes, not two transcriptions that happen to agree.

   The emission recipe that makes that true (measured, PyYAML 6.0.3): yaml.dump with a SafeDumper subclass carrying one representer for a str subclass emitted with style pipe, plus sort_keys=False, allow_unicode=True, width=10**9. Plain safe_dump does NOT reproduce it — PyYAML defaults to double-quoted escapes for multi-line strings and folds long single-line prose at width 80, so four of the five prose layers come out as quoted one-liners. width is effectively unbounded on purpose: re-wrapping prose is what makes a was-this-edited diff unreadable, and prose fidelity is what the whole port is judged on.

2. THE ENTRIES EQUAL _stickers_fixture.sticker_entries. test_the_derived_entries_equal_the_phase5_fixture_derivation asserts full dict equality, with the ONE intended difference applied to the expectation: the fixture keeps franks own anchor strings while the tool relocates them. That is the diffability the phase-5 handoff asked for, and it is what makes the phase-5 goldens a proof about the shipped tool rather than about a test helper.

Also asserted green: tools/validate_images.py reports 18 entries and no errors for the migrated blog — the only thing that catches an unresolvable composition order.

Full suite after both tasks: 1133 passed, 7 skipped, 7m11s. Baseline before this phase was 1082 passed + 7 skipped, so the phase adds 51 assertions. tests/unit/test_image_compose.py is untouched (git diff empty) and green.

<!-- fr:journal kind=discovery scope=plan id=p6-mutation-proof-goldens-are-blind-to-the-anchors created=2026-08-03T14:18:18 phase=6 -->
### p6-mutation-proof-goldens-are-blind-to-the-anchors · discovery · MEASURED: a naive transform that keeps franks anchor strings verbatim leaves all 18 goldens GREEN — spec risk 4 confirmed, and four assertions are the only thing that sees it (phase 6)

Spec risk 4 predicted that getting the self-referential style anchors wrong would let the goldens pass while generation silently loses the anchors. That is now measured, not predicted.

Twelve mutations were applied to tools/migrate_stickers.py, each run against the full phase-5 + phase-6 selection, each reverted with the file byte-restored and verified. Results, by what the mutation breaks:

BLIND TO THE GOLDENS (the important one). Making relocate() return every path unchanged — i.e. a transform that copies franks reference strings verbatim, which is the obvious naive implementation — leaves ALL 18 golden-prompt assertions GREEN and reddens exactly four: the anchors-rewritten assertion, the still-references-its-own-output assertion, the no-legacy-path assertion, and the fixture-derivation equality. The composed prompt never names a path, so prompt equality cannot see this class of defect at all. In the field the symptom would be 18 prompts that diff clean and a model that has stopped seeing the head/hair anchors.

The strongest guard against it is not a path assertion but test_every_declared_reference_path_exists_after_the_move, which runs the real --move-assets and then requires every DECLARED reference path to resolve on disk, with nothing synthesised. That is the assertion that forces the rewrite and the move to agree rather than each being individually plausible.

LOUD IN THE GOLDENS (18 red each): dropping the clothing empty table (the absent layer resolves to empty string and compose drops the section); emitting a bare mood layer instead of sticker_mood; emitting a bare order: sticker instead of composition_orders[sticker]; swapping scene and sticker_border_spec. Each also reddens the specific assertion written for it, so a future reader gets a named cause and not just 18 walls of text.

One asymmetry worth recording: a bare mood: reddens 23 assertions including the paste-and-validate test, because pasting the block then CLOBBERS franks cover mood table and validate_config still returns [] — the config is legal, it is just wrong. Only test_pasting_the_block_leaves_franks_cover_mood_table_intact and the explicit no-bare-mood regex say so.

The seven --move-assets mutations each reddened exactly the one or two tests written for that behaviour and nothing else: skip-the-conflict, clobber-the-destination, forget-src-equals-dest, forget-the-missing-source, dry-run-performs-the-move, never-use-git, no-fallback-when-git-declines. No over-coupling.

<!-- fr:journal kind=finding scope=plan id=p6-t2-could-not-be-red-move-assets-shipped-with-t1 created=2026-08-03T14:18:41 phase=6 state=open -->
### p6-t2-could-not-be-red-move-assets-shipped-with-t1 · finding [open] · P6.T2.S1 was not RED: --move-assets shipped in the T1 GREEN step because two T1 tests exercise the flag (phase 6)

Reporting a TDD deviation rather than dressing it up.

P6.T2.S1 says EXPECT: RED. It was not. The tool was written in one pass during P6.T1.S2, including --move-assets, because two assertions P6.T1.S1 asks for pass the flag: the config-is-never-edited test runs with --move-assets (the strongest form of that assertion is that even the run which moves 20 files does not touch .blog-craft.yaml) and the --dry-run test asserts the planned moves are printed and not performed. Once the flag has to exist and behave for T1, the T2 contract is already there.

I did get one genuine RED in T2, and it was mine, not the tools: test_move_assets_git_mvs_both_directories failed because build_legacy_blog only ran git init + git add. git status --porcelain reports renames only against a COMMITTED baseline — with an empty HEAD every path reads as A, so the rename assertion counted zero. Fixed by committing the legacy state in the fixture, with the reason inlined; without that commit the assertion would have been vacuous rather than wrong, which is worse.

What was done instead of a manufactured red: seven mutations of the move contract, each reverted and byte-verified, each reddening exactly the assertion written for it (see p6-mutation-proof-goldens-are-blind-to-the-anchors). Deleting the implementation and re-pasting it to produce a red would have produced a red-green transcript and no additional information.

This is the same shape as p5-t2-cannot-be-red-mapping-belongs-to-t1 and phase 2 review fix 3: coverage that lands earlier than the plan sequenced it. Recommendation for the orchestrator, plan text not code: either relabel P6.T2.S1 a coverage step, or drop --move-assets and --dry-run from P6.T1.S1 assertions 2 and 5 so T2 has something left to make green. The first is more honest — the flag genuinely has to exist before the config-is-never-edited assertion can be written at its strongest.

<!-- fr:journal kind=decision scope=plan id=p6-what-counts-as-ambiguous created=2026-08-03T14:19:14 phase=6 -->
### p6-what-counts-as-ambiguous · decision · DECIDED: what tools/migrate_stickers.py refuses, and the one case it only WARNs about (phase 6)

The plan says exit non-zero with the reason on anything ambiguous but does not enumerate the cases. Each of these is implemented, tested, and chosen for a stated reason.

REFUSALS (exit 1, message names the key or the sticker):

- An empty or whitespace-only clothing, mood or scene on any sticker. This is the sharpest one and it comes straight out of phase 5s invariant: franks join does NOT filter empty sections while compose() DOES, so the two agree only because all eight sections are non-empty for all 18. A blank field therefore composes SEVEN sections against franks eight — the transform quietly stops being prompt-preserving, and only the golden for that one sticker would notice. The message says so, and says fill it in or drop the sticker.
- A missing prose key, named.
- A duplicate sticker key: keys become the output filename sticker-<key>.png, so two records would overwrite one curated master.
- features.stickers missing or any of its three paths empty, with a pointer to run /update first so the v6-to-v7 rung seeds the block.
- Any of the three configured paths resolving OUTSIDE the blog root. The tool resolves everything against cfg_path.parent (never the process CWD — the tests invoke it from outside the blog to prove that), so an escaping path is the only way it could write somewhere the operator did not name.
- features.stickers.prompts_file resolving to the LEGACY source itself. Writing it would destroy the only copy of the prose being migrated. This one is not hypothetical: the phase-5 fixture deliberately names the prompts file stickers-prompts.yaml precisely because the legacy file is called stickers.yaml, and spec section 1s example block has prompts_file: blog/_private/stickers/stickers.yaml — an operator who keeps the legacy directory name has typed the collision.
- A reference path inside the legacy directory but in neither images/ nor sheets/, because there is no configured destination to relocate it to. Confirmed useful by mutation: it is what turns a silently-unrelocated path into an error instead of a dead reference.
- --move-assets with an existing destination. Validated for BOTH directories before ANY move, so a conflict on images/ cannot leave sheets/ relocated — a half-migrated tree would be worse than a refusal.

NOT a refusal, deliberately:

- A slug that is not derivable from the key gets a stderr NOTE and is dropped. Measured across all 18: slug is always the key minus its numeric prefix (05-golden-key -> golden-key), and NOTHING reads it — not the shim, not build-sheets.py (which reads sheet, pos, key, output). So dropping it loses no information for franks data and cannot break generation for anyone elses. Blocking a migration over a field with no consumer would be the wrong trade; saying nothing would silently discard operator text.
- Prose containing braces. NO brace guard exists, by design — see the separate finding.
- A sticker with no sheet/pos: emitted without those fields, because build-sheets.py already treats that as not on a sheet. Absent stays absent rather than becoming a guess.

Idempotence has two forms, both tested. A second identical run finds the destination byte-identical, prints unchanged, and does NOT rewrite the file (mtime asserted unchanged, no .bak). Pointing --legacy at the tools OWN output is recognised (entries under images:, no legacy stickers: list) and reported as already migrated with exit 0 — a no-op, not a double transform. When the destination exists and DIFFERS the previous copy is backed up to .bak before writing, matching tools/migrate_prompts.py.

<!-- fr:journal kind=finding scope=plan id=p6-brace-guard-refuted created=2026-08-03T14:19:33 phase=6 state=refuted -->
### p6-brace-guard-refuted · finding [refuted] · REFUTED with a test: prose containing braces needs no guard — the mood frame formats it correctly and the migrated entry carries it verbatim (phase 6)

Spec section Risks 2 (already marked withdrawn) claimed prose with literal braces was a sharp edge that tools/migrate_stickers.py would have to police. The withdrawal is now backed by an executed test rather than by reasoning about str.format.

test_prose_containing_braces_is_not_refused_and_composes_correctly feeds a mutated legacy file through the real tool, with sticker 05-golden-key given

    scene: Frank points at a screen showing {"status": "ok"} in a JSON blob.
    mood:  amused — reading {0} of {n} retries, one brow up

The tool exits 0, the entry carries both strings verbatim, and the engine composes the scene unchanged plus the mood framed as Frank\s expression: amused — reading {0} of {n} retries, one brow up. — braces and all. Note the mood deliberately contains {0} and {n}, the two forms that would raise IndexError and KeyError if the SUBSTITUTED argument were ever rescanned. It is not: _apply_template calls template.format(value), so only the template string is parsed.

So a brace guard in the transform would reject valid content for no reason, and none was built. The one-{} rule stays where it belongs, on _template in validate_config, where the phase-1 review already documented that the policy half of that rule exists so a frame has exactly one obvious spelling and is mechanically emittable — by this tool.

Measured aside: none of franks 18 stickers actually contains a brace in scene, mood or clothing today, so this is a guard against a future edit, not a live case.

<!-- fr:journal kind=discovery scope=plan id=p6-artifacts-for-phases-7-8 created=2026-08-03T14:20:05 phase=6 -->
### p6-artifacts-for-phases-7-8 · discovery · What phase 6 leaves for phases 7 and 8: the tools CLI, the runbook it implies, and three doc claims that need correcting (phase 6)

ARTIFACTS: tools/migrate_stickers.py (shipped, ~420 lines with the WHY comments the plan asked for) and tests/unit/test_migrate_stickers.py (51 assertions, 18 of them the golden equality).

THE CLI, for docs and for franks adoption PR:

    tools/migrate_stickers.py --config <blog>/.blog-craft.yaml \
        --legacy <blog>/blog/_private/frank-stickers/stickers.yaml \
        [--move-assets] [--dry-run]

Both paths are REQUIRED and nothing is guessed. Everything resolves against cfg_path.parent, never the process CWD — the tests invoke the tool from outside the blog to prove it, matching the engines root = cfg_path.parent convention. --move-assets is opt-in: a default run writes the prompts file and prints the block, and touches not one PNG.

FOR PHASE 7 (manifest.legacy_dests). Nothing here needs a manifest row: tools/ is blog-craft-side tooling that the operator runs against a blog config, exactly like tools/migrate_prompts.py and tools/migrate_config.py, and it is never shipped into a blog. Phase 7 owns retiring the two SCRIPTS. Note the division of labour the migration implies: the tool moves images/ and sheets/ out of the legacy directory, and leaves stickers.yaml and README.md behind for the operator to git rm (spec section 8 step 5) — it never deletes the operators prose.

FOR PHASE 8 (docs, CHANGELOG, matrix):

- The matrix was deliberately NOT edited, following phase 5. stk-5s levels.unit can now cite blog-craft:tests/unit/test_migrate_stickers.py for the transform half of the row; the manifest.legacy_dests half lands in phase 7, so the row should not move to ci until both exist.
- THREE CLAIMS IN THE SPEC NEED SOFTENING OR CORRECTING WHEN section 7 IS DOCUMENTED. (a) Spec section 7 step 4 says the transform git mvs images/ and sheets/ unconditionally; it is behind --move-assets, and it REFUSES rather than clobbers when a destination exists. (b) Spec section 7 step 2 says it emits the six sticker_* layers — it emits SEVEN layer keys, because clothing: {} must be present or the section vanishes from all 18 prompts; six of them are sticker_*, the seventh is the empty clothing table. (c) Spec section 1s example block sets prompts_file: blog/_private/stickers/stickers.yaml, which collides with the legacy filename if the operator keeps the directory name — the tool refuses that case, and the docs should use the phase-5 fixtures stickers-prompts.yaml spelling instead.
- Worth a CONFIG.md sentence, since it is the single easiest way to break every sticker prompt at once: a layer absent from image.layers resolves to empty string and compose() drops the section, so clothing: {} is load-bearing rather than decorative.
- The frame emission recipe is worth a line for anyone regenerating the fixture: plain yaml.safe_dump does not reproduce the committed layer block; it needs a block-scalar representer and an unbounded width (see p6-transform-reproduces-all-18-goldens).

<!-- fr:journal kind=finding scope=plan id=p6-clothing-empty-table-clobbers-franks-cover-clothing created=2026-08-03T14:43:04 phase=6 state=fixed -->
### p6-clothing-empty-table-clobbers-franks-cover-clothing · finding [fixed] · FIXED: the block emitted clothing empty-table UNCONDITIONALLY, which on paste destroys franks populated clothing layer and drops the clothing section from 85 of his 90 cover prompts (phase 6)

Raised by the orchestrator against the first phase-6 commit. Verified independently and CONFIRMED, with a sharper blast radius than reported.

MECHANISM. Spec section 8 step 3 tells the operator to paste the printed block into .blog-craft.yaml. PyYAML resolves a duplicate mapping key by silently taking the LAST one — measured: a document with a populated clothing table followed by clothing empty-table loads as the empty dict, no error, no warning. So a block that always emits clothing empty-table destroys any existing clothing layer at paste time.

MEASURED AGAINST FRANKS REAL CONFIG (read from the host; frank is not mounted in the container, and this is the one thing in phase 6 that genuinely needed it — the collision surface is a property of HIS config, not of the vendored sticker data):

- image.layers.clothing is a populated NESTED two-level table with groups generic, building, operating, papers. It declares no _select and no directives.
- 85 of his 90 cover entries carry a clothing modifier, and ALL 85 use the BRACKET form group[variant] — building[dirty] 11 times, building[apron] 10, operating[hoodie_and_sunglasses] 10, and so on.

That last fact makes the consequence a DROP rather than a garble, which the original report understated. With clothing replaced by the empty table, _resolve_modifier takes the bracket path, table.get(group) returns None, the branch returns empty string, and compose() drops the section. So 85 of 90 cover prompts silently lose their clothing sentence ENTIRELY. Same blast radius and same silence as the phase-1 mood._template defect.

THE FIX. Emission is now conditional on the target config, which the tool already reads:

- image.layers.clothing ABSENT -> emit clothing empty-table exactly as before. Still required: an unknown layer resolves to empty string and compose() drops the section, so omitting it here deletes the clothing sentence from all 18 sticker prompts.
- image.layers.clothing PRESENT -> omit the key entirely and print CLOTHING_KEPT_NOTE explaining that sticker clothing is free-form prose which _resolve_modifier returns via passthrough, so the existing layer already serves stickers, and that a second key would be last-wins.

Plus two guards that were not asked for and that the fix needs to be sound rather than merely plausible:

1. check_clothing_passthrough imports the SHIPPED tools/compose.py resolve_layer and verifies, for every sticker, that the existing clothing layer returns that stickers prose UNCHANGED. Omitting the key rests on a claim about someone elses config; this checks the claim instead of trusting it, and it catches three silent failure modes at once — a value equal to a GROUP name (named lookup hits a container, _chunk returns empty, seven sections instead of eight), a multi-step _select on the existing layer (walks entry fields sticker entries do not carry, intermediate miss returns empty for all 18), and a _template on the existing layer (would frame every stickers clothing). Refuses naming the offending sticker keys.
2. A WARN listing any REMAINING emitted key the config already defines, layers and the sticker order alike. Against frank that list is empty, which is the point. Deliberately a WARN and not a refusal: for a prose layer, replacing it with the legacy prose is what re-running the migration MEANS; only clothing needs its value preserved.

Full suite 1157 passed, 7 skipped (was 1133 + 7): +24 assertions.

<!-- fr:journal kind=finding scope=plan id=p6-sticker-goldens-structurally-cannot-see-a-cover-layer-clobber created=2026-08-03T14:43:32 phase=6 state=refuted -->
### p6-sticker-goldens-structurally-cannot-see-a-cover-layer-clobber · finding [refuted] · REFUTED: the frank-like golden run does NOT catch the clothing clobber — sticker prompts are structurally incapable of seeing it, and only a COVER-resolution assertion catches it (phase 6)

The orchestrators fix brief said test 3 (run the 18 goldens against a frank-like config with populated clothing and mood tables) was the one that mattered most, and that it would have caught the defect. Built it, then measured it. It does not, and the reason generalises.

MEASURED. With the defect restored (emit clothing unconditionally) and the frank-like config in place, the 18 frank-like golden assertions are GREEN — 0 of 18 red. Exactly two assertions go red, and neither is a golden:

- test_an_existing_clothing_layer_is_never_emitted (the block still carries the key);
- test_the_existing_cover_layers_still_RESOLVE_after_the_paste.

MECHANISM. Pasting clothing empty-table replaces the table with the empty dict. For a STICKER entry, _resolve_modifier then finds no bracket in the value, no key match in an empty table, and falls straight to the free-form passthrough — which returns the stickers own prose unchanged. So the sticker section composes correctly against a DESTROYED table. The damage lands entirely on the cover side, and sticker goldens never compose a cover.

THE GENERAL LESSON, which is what makes this worth a journal entry rather than a correction in passing: an equality proof over set A cannot detect collateral damage to set B, however faithful the fixture is. Making the fixture frank-like was necessary but not sufficient — the missing ingredient was an assertion that RESOLVES the pre-existing cover selectors after the paste. That assertion runs the shipped compose.py over the merged layers and checks building[overalls] still yields Frank wears overalls., the mood key still yields its sentence, and the sticker prose still passes through the SAME table. It is three lines and it is the only thing that sees the defect.

The frank-like golden run is KEPT even though it caught nothing here. It closes a different and real class — a collision that would affect sticker composition — and it proves the emitted block is actually pasteable into a populated config rather than only into an empty one. It was green before the fix and after; that is now recorded so a later reader does not mistake it for the guard.

CONSEQUENCE FOR PHASE 8 AND FOR ANY FUTURE PORT: the pattern to reach for when a change adds keys to a shared config is not a bigger fixture, it is an assertion that the EXISTING consumers still resolve. Phase 1 hit the same shape from the other direction (the mood._template regression was invisible to sticker goldens and to test_image_compose.py, which uses a _template-free fixture) and the resolution there was also a namespacing change plus a targeted assertion, not a wider golden set.

Four mutations pin the fix, each reverted and byte-verified: emit-unconditionally reddens exactly the 2 named above; omit-always reddens the 18 BASE-config goldens plus the emitted-when-absent test (so the required half is guarded too); trust-the-passthrough reddens only the passthrough-verification test; omit-the-explanation reddens only the note test.

<!-- fr:journal kind=discovery scope=plan id=p6-collision-surface-enumerated-and-closed created=2026-08-03T14:43:57 phase=6 -->
### p6-collision-surface-enumerated-and-closed · discovery · COLLISION SURFACE ENUMERATED against franks real config: clothing was the only clash, and it is now closed rather than narrowed (phase 6)

Asked for explicitly by the orchestrator. Every top-level key the emitted block writes, checked against frank/.blog-craft.yaml as read from the host on 2026-08-03.

WHAT THE BLOCK EMITS (7 layer keys, 1 order):
  image.layers.sticker_base_character
  image.layers.sticker_atmosphere
  image.layers.sticker_reference_guidance
  image.layers.sticker_face_pins
  image.layers.clothing              <- conditional after the fix
  image.layers.sticker_mood
  image.layers.sticker_border_spec
  image.composition_orders.sticker

WHAT FRANK HAS:
  image.layers: base_character (str), base_atmosphere (str), reference_guidance (dict: anchor, scenery_reference_image, character, single_hero, drawing_instructions, banner_lettering), clothing (dict: generic, building, operating, papers), mood (dict: curious, weighing, determined, worried, smirking, approving)
  image.composition_orders: hero, scenery, banner, banner_transform
  image.composition_order (the v4 single-order key): ABSENT

INTERSECTION: clothing, and nothing else. Verified pair by pair:

- sticker_base_character vs his base_character — different names. This is the namespacing spec section Findings 2 required for a different reason (the two prose bodies contradict each other: stickers say NOT a hard blocky square flat-top, covers say flat-top hair), and it incidentally removes the clash.
- sticker_atmosphere vs his base_atmosphere — different names. Note the legacy stickers.yaml key is already literally sticker_atmosphere, so no rename even happens here.
- sticker_reference_guidance vs his reference_guidance — different names. His is a dict addressed with bracket tokens like reference_guidance[anchor], which is untouched.
- sticker_face_pins — he has no face_pins layer at all. Wholly new.
- sticker_border_spec — wholly new.
- sticker_mood vs his mood — different names, and mood is NEVER emitted (plan step 2b, pinned twice: a parse assertion and a regex, plus the paste test that asserts his cover mood table survives).
- composition_orders.sticker — not among hero / scenery / banner / banner_transform, and there is no v4 composition_order key to clash with either.

So the surface is CLOSED for frank, not merely narrowed: after the clothing fix, a literal paste of the block into his config replaces nothing.

And it is closed for blogs other than frank too, by a guard rather than by inspection: the tool now WARNs listing any emitted key the target config already defines (layers and the sticker order both). A WARN and not a refusal, because for a prose layer being replaced by the legacy prose is what re-running the migration MEANS — only clothing needs its existing value preserved, and that key is omitted outright. Pinned from both sides: test_frank_has_no_other_colliding_key_so_the_run_is_warning_free asserts a frank-shaped run emits no WARN at all, and test_a_key_the_config_already_defines_is_warned_about plants a stale sticker_border_spec layer plus a stale sticker order and asserts both are named.
